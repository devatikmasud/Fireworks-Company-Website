document.addEventListener('DOMContentLoaded',()=>{
 const pre=document.querySelector('.preloader');setTimeout(()=>pre?.classList.add('hide'),650);
 const nav=document.querySelector('.nav-main');const onScroll=()=>nav?.classList.toggle('scrolled',window.scrollY>70);window.addEventListener('scroll',onScroll,{passive:true});onScroll();
 const items=document.querySelectorAll('.reveal');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');io.unobserve(e.target)}}),{threshold:.12});items.forEach((el,i)=>{el.style.transitionDelay=Math.min(i%5*.08,.32)+'s';io.observe(el)});
 document.querySelectorAll('[data-count]').forEach(el=>{const target=+el.dataset.count;let done=false;const obs=new IntersectionObserver(es=>{if(es[0].isIntersecting&&!done){done=true;let start=0;const step=Math.max(1,Math.ceil(target/45));const tick=()=>{start=Math.min(target,start+step);el.textContent=start.toLocaleString();if(start<target)requestAnimationFrame(tick)};tick();obs.disconnect()}});obs.observe(el)});
 const lb=document.getElementById('lightbox');const lbimg=lb?.querySelector('img');document.querySelectorAll('.gallery-item').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();if(lb&&lbimg){lbimg.src=a.href;lb.classList.add('open');document.body.style.overflow='hidden'}}));const close=()=>{lb?.classList.remove('open');document.body.style.overflow=''};document.querySelector('.lightbox-close')?.addEventListener('click',close);lb?.addEventListener('click',e=>{if(e.target===lb)close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});
 document.querySelectorAll('.quote-form').forEach(form=>form.addEventListener('submit',()=>{const btn=form.querySelector('button[type=submit]');if(btn){btn.disabled=true;btn.innerHTML='<span class="spinner-border spinner-border-sm me-2"></span> Sending...'}}));
});
